# Minimal deps for proof-of-concept (YOLO + ByteTrack via Ultralytics).
# On HiPerGator (from this directory):
#   source setup_env.sh
#   install_mvp_deps    # first time only
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
pyyaml>=6.0
lap>=0.5.12
